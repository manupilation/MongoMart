name: Eslint Backend
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install dependencies
      run: cd server && npm install
      with:
        cache: npm

    - name: Run tests
      run: npm run lint